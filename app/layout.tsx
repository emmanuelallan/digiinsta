// Root layout - delegates to route group layouts
// This minimal layout is required by Next.js but actual layouts are in route groups

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
